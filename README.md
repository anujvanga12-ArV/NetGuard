NetGuard is a web-based network security analysis tool built in Python. It evaluates a user's connection by checking HTTPS enforcement, detecting VPN usage, and pulling IP metadata through public geolocation APIs. Based on those factors, it generates a risk score that classifies the user's exposure level, similar to how enterprise tools assess network trustworthiness. Built to deepen my understanding of how network security tools gather and interpret connection data in real time.
